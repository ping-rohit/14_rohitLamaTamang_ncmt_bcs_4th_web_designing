# Mockups

High-fidelity mockups (colored, styled previews of each screen) go in this folder.

The live site (`index.html`) currently serves as the working mockup — this
folder is reserved for exported screenshots or design-tool mockups if you
create them separately later.
